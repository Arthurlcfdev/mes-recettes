# Chaine de connexion Postgres poolee (Neon ou Vercel Postgres)
POSTGRES_URL=

# Mot de passe partage unique pour acceder a l'application
APP_PASSWORD=

# Secret aleatoire utilise pour signer le cookie de session (ex: openssl rand -base64 32)
SESSION_SECRET=
